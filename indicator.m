% Indicator function. Takes in start and end values and produces a 1 unit
% high rectangle from start to end.

function [vector] = indicator(start, stop, resolution)
    vector = zeros(1, (stop - 0)/resolution);
    
end
